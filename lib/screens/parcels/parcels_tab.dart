import 'package:flutter/material.dart';

class ParcelsTab extends StatelessWidget {
  static const String routeName = '/parcels';

  const ParcelsTab({super.key});

  @override
  Widget build(BuildContext context) {
    return const Text('PARCELS');
  }
}
